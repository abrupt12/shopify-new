{%- layout 'theme' -%}

<div class="container mx-auto px-4 py-16 text-center">
  <h1 class="text-4xl font-bold mb-4">{{ 'general.404.title' | t }}</h1>
  <p class="text-lg mb-8">{{ 'general.404.subtext' | t }}</p>
  <a href="/" class="inline-block bg-primary text-white px-8 py-3 rounded-lg hover:bg-opacity-90 transition-colors">
    {{ 'general.404.button' | t }}
  </a>
</div> 